<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title | capitalize }} — Brad Oyler</title>
  <link rel="stylesheet" href="/assets/style.css">
</head>
<body class="bg-white dark:bg-gray-950 text-gray-900 dark:text-gray-100 min-h-screen">

  <header class="border-b border-gray-200 dark:border-gray-800">
    <div class="max-w-5xl mx-auto px-6 py-6 flex justify-between items-center">
      <a href="/" class="text-2xl font-bold tracking-tight">Brad Oyler</a>
      <nav class="space-x-8 text-sm font-medium">
        <a href="/" class="hover:text-blue-600 dark:hover:text-blue-400">Home</a>
        <a href="/blog/" class="hover:text-blue-600 dark:hover:text-blue-400">Blog</a>
        <a href="/projects/" class="hover:text-blue-600 dark:hover:text-blue-400">Projects</a>
        <a href="/about/" class="hover:text-blue-600 dark:hover:text-blue-400">About</a>
      </nav>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-6 py-12">

    <h1 class="text-4xl font-bold mb-12">{{ title | capitalize }}</h1>

    <section class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
      {% for post in collections[page.fileSlug] %}
        <article class="border border-gray-200 dark:border-gray-800 rounded-lg p-6 hover:shadow-lg transition">
          <h2 class="text-xl font-semibold mb-2">
            <a href="{{ post.url }}">{{ post.data.title }}</a>
          </h2>
          <time class="text-sm text-gray-600 dark:text-gray-400" datetime="{{ post.date }}">
            {{ post.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) }}
          </time>
          {% if post.data.description %}
            <p class="mt-3 text-gray-700 dark:text-gray-300">{{ post.data.description }}</p>
          {% endif %}
          <a href="{{ post.url }}" class="inline-block mt-4 text-blue-600 dark:text-blue-400 hover:underline text-sm font-medium">
            Read more
          </a>
        </article>
      {% endfor %}
    </section>

  </main>

</body>
</html>