<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} — {% endif %}Brad Oyler</title>
  <meta name="description"
        content="{% if description %}{{ description }}{% else %}Personal site of Brad Oyler.{% endif %}">
  <link rel="stylesheet" href="/assets/style.css">
</head>

<body class="bg-white dark:bg-gray-950 text-gray-900 dark:text-gray-100 min-h-screen flex flex-col">

  {% include "layouts/_includes/header.njk" %}

  <main class="max-w-5xl mx-auto px-6 py-12 flex-1">
    
    {# --- Page Content Area --- #}
    <h1 class="text-4xl font-extrabold mb-10 border-b border-gray-200 dark:border-gray-800 pb-4">
      {{ title | capitalize }}
    </h1>

    {# Tailwind Typography for styling Markdown content (e.g., in /about/) #}
    <div class="prose prose-xl dark:prose-invert max-w-none 
                prose-a:text-blue-600 dark:prose-a:text-blue-400">
      {{ content | safe }}
    </div>

  </main>

{% include "layouts/_includes/footer.njk" %}
</body>
</html>